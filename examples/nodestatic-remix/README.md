# Remix Static Site

This fixture mirrors the static-output shape of the Remix example in the
sibling examples repository: a build step prepares `public/`, then Shipit
serves those files with `static-web-server`.
