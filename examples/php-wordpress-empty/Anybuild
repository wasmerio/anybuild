# Generated by Anybuild v0.23.0 — https://anybuild.run
# This file is yours to edit: add steps or override the serve below.

load("//anybuild/tools:wordpress.bzl", "wordpress_build", "wordpress_config", "wordpress_serve")
load("//anybuild:services.bzl", "mysql")

config = wordpress_config(
    schema = 1,
    services = [mysql()],
    php_version = "8.3.29",
)

build = wordpress_build(config)

wordpress_serve(config, build, name = "php-wordpress-empty")
