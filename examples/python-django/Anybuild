# Generated by Anybuild v0.23.0 — https://anybuild.run
# This file is yours to edit: add steps or override the serve below.

load("//anybuild/tools:python.bzl", "python_build", "python_config", "python_serve")

config = python_config(
    schema = 1,
    commands = {
        "start": "uvicorn mysite.wsgi:application --interface=wsgi --host 0.0.0.0 --port $PORT",
    },
    services = [
        {
            "name": "database",
            "engine": "mysql",
        },
    ],
    python_framework = "django",
    python_server = "uvicorn",
    python_migration_strategy = "django",
    python_extra_dependencies = [
        "uvicorn",
    ],
    wsgi_application = "mysite.wsgi:application",
    python_version = "3.13",
    uv_version = "0.8.15",
)

build = python_build(config)

python_serve(config, build, name = "python-django")
