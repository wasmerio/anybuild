# Generated by Anybuild v0.23.0 — https://github.com/wasmerio/anybuild
# This file is yours to edit: add steps or override the serve below.

load("//anybuild/tools:staticfile.bzl", "staticfile_build", "staticfile_config", "staticfile_serve")

config = staticfile_config(
    schema = 1,
    sws_version = "2.38.0",
    static_dir = "site",
)

build = staticfile_build(config)

staticfile_serve(config, build, name = "staticfile-redirects")
