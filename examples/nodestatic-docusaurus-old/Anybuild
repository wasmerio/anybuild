# Generated by Anybuild v0.23.0 — https://anybuild.run
# This file is yours to edit: add steps or override the serve below.

load("//anybuild/tools:node_static.bzl", "nodestatic_build", "nodestatic_config", "nodestatic_serve")

config = nodestatic_config(
    schema = 1,
    sws_version = "2.38.0",
    static_dir = "build",
    node_package_manager = "npm",
    node_framework = "docusaurus-old",
    node_server = "node",
    node_build_command = "npm run build",
    node_version = "24",
)

build = nodestatic_build(config)

nodestatic_serve(config, build, name = "nodestatic-docusaurus-old")
