[package]
name = "shipit-e2e"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shipit end-to-end suite: drives the shipit binary against the examples"
publish = false

[dependencies]
anyhow.workspace = true
libc = "0.2"
regex = "1"
ureq = "2"
